import { Mukta } from 'next/font/google';

export const regular = Mukta({ subsets: ['latin'], weight: '400' });

export const bold = Mukta({ subsets: ['latin'], weight: '700' });

export const extraBold = Mukta({ subsets: ['latin'], weight: '800' });
